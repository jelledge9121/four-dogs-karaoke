"use client";import {useEffect,useState} from "react";import Brand from "@/components/Brand";import {createSupabaseBrowser} from "@/lib/supabase";const sb=createSupabaseBrowser();export default function Display({params}:{params:Promise<{slug:string}>}){const[slug,setSlug]=useState(""),[state,setState]=useState<any>(null);useEffect(()=>{let c:any;params.then(async x=>{setSlug(x.slug);await load(x.slug);if(sb)c=sb.channel(`display-${x.slug}`).on("postgres_changes",{event:"*",schema:"public",table:"requests"},()=>load(x.slug)).on("postgres_changes",{event:"*",schema:"public",table:"rotation_entries"},()=>load(x.slug)).subscribe()});return()=>{if(c&&sb)sb.removeChannel(c)}},[params]);async function load(s:string){if(!sb)return;const{data}=await sb.rpc("get_public_display",{p_slug:s});setState(data)}const p=state?.playing,n=state?.next;return <main className="display"><div>{p?.videoId?<><iframe key={p.videoId} className="video" src={`https://www.youtube.com/embed/${encodeURIComponent(p.videoId)}?autoplay=1&enablejsapi=1`} allow="autoplay; encrypted-media; picture-in-picture" allowFullScreen/><p className="status">NOW SINGING</p><h2>{p.singer}</h2><p>{p.title}</p>{n&&<><p className="status">UP NEXT</p><h3>{n.singer} · {n.title}</h3></>}</>:<><Brand/><h1>FOUR DOGS KARAOKE</h1><p>Scan to request a song</p>{slug&&<img src={`/api/qr?slug=${encodeURIComponent(slug)}`} width="220" alt="Event QR"/>}<p className="status">UP NEXT</p><h2>{n?.singer||"Get your request in!"}</h2>{n?.title&&<p>{n.title}</p>}</>}</div></main>}