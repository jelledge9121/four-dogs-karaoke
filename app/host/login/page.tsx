"use client";import {useState} from "react";import Brand from "@/components/Brand";import {createSupabaseBrowser} from "@/lib/supabase";export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function login(){const sb=createSupabaseBrowser();if(!sb){setMsg("Supabase is not configured.");return}const{error}=await sb.auth.signInWithPassword({email,password});if(error){setMsg(error.message);return}window.location.href="/host/events/TEST123"}return <main className="shell"><Brand/><div className="card"><h1>Host Login</h1><label>Email</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn" onClick={login}>SIGN IN</button>{msg&&<p>{msg}</p>}</div></main>}