"use client";
import {useEffect,useMemo,useState} from "react";import Brand from "@/components/Brand";import {createSupabaseBrowser} from "@/lib/supabase";
const sb=createSupabaseBrowser();type Row={id:string,status:string,created_at:string,provider_payload:any,singers:any,songs:any};const info=(r:Row)=>({singer:r.singers?.display_name||"Singer",title:r.songs?.title||r.provider_payload?.title||"Unknown song",artist:r.songs?.artist||r.provider_payload?.channel||"",videoId:r.songs?.provider_song_id||r.provider_payload?.videoId||"",thumbnail:r.songs?.thumbnail||r.provider_payload?.thumbnail||"",source:r.provider_payload?.provider==="youtube"?"YOUTUBE":"CURATED"});
export default function Host({params}:{params:Promise<{slug:string}>}){const[slug,setSlug]=useState(""),[event,setEvent]=useState<any>(null),[rows,setRows]=useState<Row[]>([]),[rotation,setRotation]=useState<any[]>([]),[msg,setMsg]=useState("Loading…");
 useEffect(()=>{let channel:any;params.then(async x=>{setSlug(x.slug);if(!sb)return;const {data:{session}}=await sb.auth.getSession();if(!session){location.href="/host/login";return}await load(x.slug);channel=sb.channel(`host-${x.slug}`).on("postgres_changes",{event:"*",schema:"public",table:"requests"},()=>load(x.slug)).on("postgres_changes",{event:"*",schema:"public",table:"rotation_entries"},()=>load(x.slug)).subscribe()});return()=>{if(channel&&sb)sb.removeChannel(channel)}},[params]);
 async function load(s:string){if(!sb)return;const {data:e,error:ee}=await sb.from("events").select("id,name,slug,requests_open,average_song_minutes,venues(name)").ilike("slug",s).single();if(ee){setMsg(ee.message);return}setEvent(e);const {data:r,error}=await sb.from("requests").select("id,status,created_at,provider_payload,singers(display_name),songs(title,artist,provider_song_id,thumbnail)").eq("event_id",e.id).order("created_at");if(error){setMsg(error.message);return}setRows((r||[]) as any);const {data:re}=await sb.from("rotation_entries").select("request_id,round,position").eq("event_id",e.id).order("position");setRotation(re||[]);setMsg("")}
 async function status(id:string,s:string){if(!sb)return;const {error}=await sb.rpc("host_set_request_status",{p_request_id:id,p_status:s});if(error)setMsg(error.message);else await load(slug)}async function play(id:string){if(!sb)return;const {error}=await sb.rpc("host_start_request",{p_request_id:id});if(error)setMsg(error.message);else await load(slug)}async function toggle(){if(!sb||!event)return;const{error}=await sb.from("events").update({requests_open:!event.requests_open}).eq("id",event.id);if(error)setMsg(error.message);else await load(slug)}
 const pending=rows.filter(r=>r.status==="PENDING"),playing=rows.find(r=>r.status==="PLAYING"),done=rows.filter(r=>r.status==="COMPLETED"),byId=useMemo(()=>new Map(rows.map(r=>[r.id,r])),[rows]),queue=rotation.map(x=>({...x,row:byId.get(x.request_id)})).filter(x=>x.row);
 if(!event)return <main className="shell"><Brand/><div className="card">{msg}</div></main>;return <main className="shell"><Brand/><div className="row"><h1>{event.name}</h1><span className="spacer"/><button className="btn secondary" onClick={toggle}>{event.requests_open?"Pause Requests":"Resume Requests"}</button></div><div className="nav"><a className="btn secondary" href={`/e/${slug}`} target="_blank">Guest Page</a><a className="btn secondary" href={`/display/${slug}`} target="_blank">Audience Display</a><a className="btn secondary" href={`/host/events/${slug}/qr`}>QR Code</a></div>{msg&&<div className="card">{msg}</div>}<h2>Now Singing</h2><div className="card">{playing?<><h3>{info(playing).singer}</h3><p>{info(playing).title}</p><button className="btn" onClick={()=>status(playing.id,"COMPLETED")}>Complete Song</button></>:<p className="muted">Waiting for host.</p>}</div><h2>New Requests</h2>{pending.map(r=>{const d=info(r);return <div className="card song" key={r.id}>{d.thumbnail&&<img src={d.thumbnail} alt=""/>}<div><h3>{d.singer}</h3><p>{d.title} · {d.artist}</p><span className="pill">{d.source==="YOUTUBE"?"UNVERIFIED YOUTUBE":"CURATED"}</span>{d.videoId&&<p><a href={`https://www.youtube.com/watch?v=${encodeURIComponent(d.videoId)}`} target="_blank">Preview on YouTube</a></p>}</div><span className="spacer"/><div><button className="btn" onClick={()=>status(r.id,"APPROVED")}>Approve</button> <button className="btn danger" onClick={()=>status(r.id,"DECLINED")}>Decline</button></div></div>})}<h2>Current Rotation</h2><div className="queue">{queue.map(x=>{const d=info(x.row);return <div className="card" key={x.row.id}><span className="status">#{x.position} · Round {x.round}</span><h3>{d.singer}</h3><p>{d.title}</p><button className="btn" onClick={()=>play(x.row.id)}>CUE / PLAY</button></div>})}</div><h2>Completed</h2>{done.slice().reverse().map(r=><div className="card" key={r.id}>{info(r).singer} · {info(r).title}</div>)}</main>}