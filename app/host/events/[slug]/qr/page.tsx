"use client";import {useEffect,useState} from "react";import Brand from "@/components/Brand";export default function QR({params}:{params:Promise<{slug:string}>}){const[slug,setSlug]=useState("");useEffect(()=>{params.then(x=>setSlug(x.slug))},[params]);return <main className="shell"><Brand/><div className="card"><h1>Event QR</h1>{slug&&<img src={`/api/qr?slug=${encodeURIComponent(slug)}`} width="320" alt="Guest request QR"/>}<p>Guests scan this code to request songs.</p></div></main>}